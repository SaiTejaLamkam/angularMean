const User = require('../auth/auth.modal');
const UserFriends = require('./userFriends.modal');
const bcrypt = require('bcrypt');
const jwt = require('jsonwebtoken');

exports.sendRequest = async function (req, res) {
	console.log(req.body, '))))))))))))))))')
};

